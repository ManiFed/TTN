# Marketing site (thetelescope.net) — static tour + /download/* → API
FROM nginx:alpine
COPY website/ /usr/share/nginx/html/
COPY nginx.website.conf /etc/nginx/conf.d/default.conf
CMD sh -c "sed -i \"s/PORT_PLACEHOLDER/${PORT:-80}/\" /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"
