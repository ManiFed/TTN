# Flutter / Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock

# Generated platform folders (run `flutter create .` to regenerate)
# Comment these out if you commit platform code.
android/
ios/
linux/
macos/
windows/

# web/ source is tracked (manifest, index.html, icons)
# build/web/ output is tracked so Fly.io can serve it without a build step
!build/web/

# IDE
.idea/
*.iml
.vscode/
